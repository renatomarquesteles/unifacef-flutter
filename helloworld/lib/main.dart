import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    home: Scaffold(
        appBar: AppBar(
          title: Text('Meu Primeiro App'),
        ),
        body: Text('Hello world :)')),
  ));
}
